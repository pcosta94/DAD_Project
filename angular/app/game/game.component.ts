import { Component, Input, OnInit } from '@angular/core';

import { Maos } from './maos';

@Component({
	moduleId: module.id,
	selector: 'game',
	templateUrl: 'game.component.html'
})

export class GameComponent implements OnInit {

	
}